import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Map from './Map/';

ReactDOM.render(
  // <React.StrictMode>
    <div className="App">
      <Map/>
    </div>,
  // </React.StrictMode>,
  document.getElementById('root')
);
